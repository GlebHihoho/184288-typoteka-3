'use strict';

const articleSchema = require(`./article`);
const commentSchema = require(`./comment`);
const userSchema = require(`./user`);

module.exports = {
  articleSchema,
  commentSchema,
  userSchema,
};
